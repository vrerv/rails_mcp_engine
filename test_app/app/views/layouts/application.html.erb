<!DOCTYPE html>
<html>
  <head>
    <title>Rails MCP Manual Test</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; margin: 2rem; color: #111; }
      textarea { width: 100%; min-height: 180px; font-family: ui-monospace, SFMono-Regular, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace; }
      input, select, textarea { padding: 0.5rem; margin-top: 0.25rem; border: 1px solid #ccc; border-radius: 6px; font-size: 16px; }
      .card { border: 1px solid #e5e5e5; border-radius: 10px; padding: 1.25rem; margin-bottom: 1.5rem; background: #fafafa; }
      .card h2 { margin-top: 0; }
      .actions { margin-top: 0.75rem; }
      pre { background: #1f2937; color: #f9fafb; padding: 0.75rem; border-radius: 8px; overflow: auto; }
      .pill { display: inline-block; padding: 0.25rem 0.6rem; border-radius: 999px; background: #eef2ff; color: #3730a3; font-size: 0.9rem; margin-right: 0.35rem; }
      
      /* Tool list styles */
      .tool-list { list-style: none; padding: 0; }
      .tool-item { margin-bottom: 0.75rem; border: 1px solid #d1d5db; border-radius: 8px; background: white; overflow: hidden; }
      .tool-header { padding: 0.75rem 1rem; display: flex; align-items: center; transition: background 0.2s; gap: 0.5rem; }
      .tool-header:hover { background: #f9fafb; }
      .tool-header strong { flex: 1; margin-left: 0.5rem; }
      .delete-tool-btn { padding: 0.35rem 0.75rem; background: #ef4444; color: white; border: none; border-radius: 6px; font-size: 0.85rem; font-weight: 500; cursor: pointer; transition: background 0.2s; }
      .delete-tool-btn:hover { background: #dc2626; }
      .expand-icon { color: #6b7280; font-size: 0.8rem; margin-left: 0.5rem; }
      .tool-details { padding: 0 1rem 1rem 1rem; background: #fafafa; border-top: 1px solid #e5e7eb; }
      .detail-section { margin-top: 1rem; }
      .detail-section h4 { margin: 0 0 0.5rem 0; color: #374151; font-size: 0.95rem; }
      .no-params { color: #6b7280; font-style: italic; margin: 0; }
      
      /* Parameter table styles */
      .params-table { width: 100%; border-collapse: collapse; font-size: 0.9rem; }
      .params-table th { text-align: left; padding: 0.5rem; background: #f3f4f6; border-bottom: 2px solid #d1d5db; font-weight: 600; color: #374151; }
      .params-table td { padding: 0.5rem; border-bottom: 1px solid #e5e7eb; }
      .params-table code { background: #f3f4f6; padding: 0.15rem 0.4rem; border-radius: 4px; font-size: 0.85rem; }
      .type-badge { display: inline-block; padding: 0.15rem 0.5rem; border-radius: 4px; background: #dbeafe; color: #1e40af; font-size: 0.8rem; font-weight: 500; }
      .enum-row td { background: #fffbeb; font-size: 0.85rem; padding: 0.4rem 0.5rem; }
      .children-row td { background: #f0fdf4; font-size: 0.85rem; padding: 0.5rem; }
      .nested-params { margin: 0.5rem 0 0 0; padding-left: 1.5rem; }
      .nested-params li { margin-bottom: 0.25rem; }
      
      /* Navigation styles */
      .main-nav { display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; padding-bottom: 1rem; border-bottom: 2px solid #e5e7eb; }
      .main-nav h1 { margin: 0; font-size: 1.5rem; }
      .nav-links { display: flex; gap: 1rem; }
      .nav-link { padding: 0.5rem 1rem; background: #eef2ff; color: #3730a3; text-decoration: none; border-radius: 6px; font-weight: 500; transition: background 0.2s; }
      .nav-link:hover { background: #dbeafe; }
    </style>
  </head>
  <body>
    <nav class="main-nav">
      <h1>Rails MCP Engine Manual Playground</h1>
      <div class="nav-links">
        <a href="/rails_mcp_engine/playground" class="nav-link">MCP</a>
        <a href="/rails_mcp_engine/chat" class="nav-link">Chat</a>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
